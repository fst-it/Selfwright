---
description: Draft or check a co-piloted cover letter for an application
---

Follow the `cover` skill's instructions for the application directory given here (assemble the
prompt if `cover-letter.md` doesn't exist yet, draft it, then run `--check` and iterate until
clean): $ARGUMENTS
