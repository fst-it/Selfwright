---
description: Apply a tailoring overlay to a CV, producing a tailored CV JSON
---

Follow the `tailor` skill's instructions to run `pnpm selfwright tailor` with these arguments (CV
path, overlay path, and output path — ask for whichever is missing): $ARGUMENTS
