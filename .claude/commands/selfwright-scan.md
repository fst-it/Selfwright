---
description: Scan configured job sources for new postings and update the pipeline queue
---

Follow the `scan` skill's instructions to run `pnpm selfwright scan` and report the results (fetched/
deduped/expired/queued counts, and any target-level warnings). $ARGUMENTS
