---
description: Generate a co-piloted content-topic digest or application-specific topics
---

Follow the `topics` skill's instructions for the archetype or application given here (run the
deterministic candidate selection, research each candidate on the live web with a 30-day freshness
window, produce the digest with the required headings and citations, then validate with
`--check` and iterate until clean): $ARGUMENTS
