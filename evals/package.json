{
  "name": "@selfwright/evals",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval": "tsx src/index.ts",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@selfwright/core": "workspace:*",
    "@selfwright/shared-config": "workspace:*",
    "@selfwright/adapter-llm-claude-cli": "workspace:*",
    "@selfwright/adapter-llm-ollama": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.0.0",
    "vitest": "^3.0.0"
  }
}
